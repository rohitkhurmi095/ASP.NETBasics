@model List<ASP.NETWebApp.Models.Category>

@{
    ViewBag.Title = "Index";
}

<h2>Categories</h2>

<table class="table">
    <thead>
        <tr>
            <th>Category Id</th>
            <th>Category Name</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(temp=> item.CategoryId)</td>
                <td>@Html.DisplayFor(temp=> item.CategoryName)</td>
            </tr>
        }
    </tbody>
</table>